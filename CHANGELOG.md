# Changelog

## [Unreleased]

### Added
- Spekta IR から Markdown ファイルを生成
- ページごとに個別の `.md` ファイルを出力
- セクション階層を見出しレベル（h1-h4）にマッピング
- 画像ファイルを `images/` にコピーし相対パスで参照
- 新 IR ノード対応: text, code, callout (note/warning/tip), list
- Mermaid グラフ、手順リスト（steps）、関連ページリンク（see）、補足（why）に対応
